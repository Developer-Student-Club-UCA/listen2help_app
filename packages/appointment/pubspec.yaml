name: appointment
description: Dart package to manage appointment data and domain layers.
version: 0.0.1+1

environment:
  sdk: ">=2.9.0 <3.0.0"

dependencies:
  # Scope Packages
  errors:
    git:
      url: git://github.com/NoScopeDevs/errors
  network_manager:
    git:
      url: git://github.com/NoScopeDevs/network_manager

  cypher:
    path: ../cypher
  client:
    path: ../client

  equatable: ^1.2.6
  dartz: ^0.9.2
  meta:
  path:

dev_dependencies:
  # code generators
  build_runner:
  json_serializable: ^3.5.1

  # testing
  mockito:
  test:

  # static analysis
  very_good_analysis:
